// Bumped by hand on every commit that reaches main — see README "Releasing a
// change" for the checklist. VERSION follows semver-ish (major.minor.patch);
// BUILD is date.sequence so two deploys on the same day stay distinguishable.
window.APP_VERSION = '1.3.0';
window.APP_BUILD = '2026.09.15.2';
